const LocalStorage = require('../../../src/service/Storage/LocalStorage');

describe('Local Storage class', () => {
    it('Test storage class object default provider', () => {
        
    });
    it('Test storage class object google provider', () => {
        
    });
});